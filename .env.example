# Environment example for Office NFL Pickems
HOST_PORT=8000
# Above controls the host port for Docker Compose mapping (${HOST_PORT}:8000)
PICKEMS_ENV=production
PICKEMS_LOG_LEVEL=INFO
# SQLite by default in ./data
# PICKEMS_DATABASE_URL=sqlite:///data/app.db
# Override secret key in production (keep private)
# PICKEMS_SECRET_KEY=change_me
# Session cookie name (optional)
# PICKEMS_SESSION_COOKIE_NAME=pickems_session
# Timezone (use IANA TZ or 'local')
# PICKEMS_TIMEZONE=local
# Backups retention (how many latest archives to keep)
# PICKEMS_BACKUPS_KEEP_LATEST=12

# NFL Data Provider
# 'espn' (default) uses ESPN public APIs for schedules and team logos.
# 'local_dict' uses only the built-in static dictionary and offline SVGs.
PICKEMS_NFL_PROVIDER=espn
# Optional: override base URL for ESPN scoreboard (normally leave unset)
# PICKEMS_NFL_API_BASE=https://site.api.espn.com/apis/v2/sports/football/nfl/scoreboard

# Live data cache TTL (seconds) for ESPN live scoreboard
# Lower for faster updates, higher to reduce API calls
# PICKEMS_LIVE_CACHE_TTL_SECONDS=15

# Suppress repeated 404/410 logs by negative-caching bad ESPN event IDs (seconds)
# PICKEMS_LIVE_NEGATIVE_TTL_SECONDS=600
